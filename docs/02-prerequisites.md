# Prerequisites

Install Terraform, Azure CLI, Git, and PowerShell. Confirm access to all four Azure subscriptions.

Run:

```powershell
./scripts/validate-prereqs.ps1
```

You need a service principal or federated identity with enough rights to create the resources in each subscription.
